<h2><%= @poll.question %></h2>
<p>here are the results of <%= pluralize @poll.votes.count, 'vote' %> </p>
  <ul>
	<% @poll.answers.each do |answer| %>
	  <li><%= answer.name %>: <%= answer.votes.count %></li>
	<% end %>
  </ul> 

<%= link_to 'Back', polls_path %>
